<Window x:Class="MusicSorter.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:MusicSorter.Properties"
        xmlns:local="clr-namespace:MusicSorter"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.About}" Height="200" Width="350" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ShowInTaskbar="False">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="/Resources/MusicSorter2_100px.png" Width="100" Height="100"  />
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="{x:Static p:Resources.Title}" />
        </StackPanel>
    </Grid>
</Window>
